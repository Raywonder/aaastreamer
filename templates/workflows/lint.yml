name: Lint
on:
  workflow_call:
  push:
    branches: [ main, master ]
  pull_request:
jobs:
  markdown-basic-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: find . -name "*.md" -type f | sort
