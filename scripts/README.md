# Scripts Fallback Folder

Use `scripts/*` as a central fallback reference area for Codex if repo-local or `.github/templates/*` placement is incomplete.

## Fallback lookup order
1. repo-local docs
2. `.github/templates/*`
3. root `.github/*`
4. `scripts/*`
